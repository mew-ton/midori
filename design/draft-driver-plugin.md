# ドライバープラグイン構造 — 技術検討インデックス

> ステータス：草稿・未確定  
> 現行の設計ドキュメントには未反映。レビュー後に反映する。

---

## ドキュメント一覧

| ファイル | 内容 |
|---|---|
| [draft-driver-sdk.md](./draft-driver-sdk.md) | ドライバーの概念・共有メモリ SPSC 通信・Driver SDK・多言語対応・バイナリ配布 |
| [draft-widget-render.md](./draft-widget-render.md) | ウィジェット（接続設定 UI）・描画コンポーネント（プレビュー外付け） |
| [draft-device-config-type.md](./draft-device-config-type.md) | Device Config Type の概念・osc-vrchat の立ち位置変更・プラグイン種別まとめ |

---

## 全体概要

```
プラグイン種別          実行形態                   イベント通信
────────────────────────────────────────────────────────────
ドライバー（全て）      サブプロセス               共有メモリ SPSC
  ↑ Driver SDK で開発。Rust / Python / Node.js / C++ 等で実装可能

Device Config Type      なし（YAML のみ）          —
  ↑ osc-vrchat はこれに分類

描画コンポーネント      GUI プロセス内 Web Component  dataset
  ↑ Shadow DOM 制約によりセキュリティを確保
```

ブリッジ本体はドライバー実装を持たない。MIDI・OSC も公式プラグインとして外部プロセスで動作する。

---

## ユーザー体験

### インストールの障壁

ドライバーのインストールに npm・git が絡む。楽器奏者・VRChat ユーザーには馴染みが薄い可能性がある。

- GUI がインストール操作をすべて隠蔽する（URL を貼るだけで完結）ことで障壁を下げる
- エラー発生時（npm 失敗・ドライバークラッシュ等）は平易な日本語で原因と対処を示す

### プラグイン開発者の体験

ドライバー開発者がローカルでテストするには、ブリッジを起動して接続確認する手順が必要になる。

- SDK がモック用のテストスタブ（ブリッジを模倣するツール）を提供できると開発サイクルが短くなる
- ドライバーからのログが Midori のイベントログに表示されると、デバッグが容易になる

---

## エコシステム・コミュニティ

| 観点 | 内容 |
|---|---|
| 発見性 | どこにプラグインを公開するか。公式カタログ・GitHub トピック等 |
| 参考実装 | 公式プラグイン（`@midori/driver-midi` 等）を OSS 公開することで、コミュニティの手本になる |
| 品質シグナル | スター数・DL 数・メンテナンス状況などをインストール画面に表示できると信頼の判断材料になる |
| 将来のサンドボックス | エコシステムが成熟したら、パーミッション宣言やコード署名の仕組みを検討する |

---

## 検討優先度

| 観点 | 重要度 | 今決めるべきか |
|---|---|---|
| 共有メモリのセキュリティ（UUID・バリデーション） | 高 | ✅ 実装に直結 |
| SDK バージョンハンドシェイク | 高 | ✅ 設計に含めるべき |
| ドライバークラッシュ検出・後片付け | 高 | ✅ 設計に含めるべき |
| インストール時の警告表示 | 高 | ✅ 設計済み |
| コードサンドボックス | 中 | 🔜 将来検討 |
| インストール UX（操作の隠蔽） | 中 | 🔜 GUI 設計時に |
| SDK テストスタブ | 中 | 🔜 開発ツール整備時に |
| プラグイン発見性・カタログ | 低 | 🔜 エコシステム成熟後 |
