[package]
name = "midori-sdk"
version = "0.1.0"
edition = "2021"
description = "Driver SDK for the Midori signal bridge"
license = "MIT OR Apache-2.0"
repository = "https://github.com/mew-ton/midori"

[dependencies]
midori-core = { workspace = true }
